# OpenMediaVault_Containers
A collection of docker container files for my OMV setup

Notes:
- The default password for Deluge is "deluge"
- When linking *arr apps, use the IP address to replace localhost.
